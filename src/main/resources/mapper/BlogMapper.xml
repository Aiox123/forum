<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.nean.forum.mapper.BlogMapper">

    <insert id="saveBlog">
        INSERT INTO blog (id, user_id, shop_id,title, images, content, liked, comments, create_time, update_time)
        VALUE
        (   #{blog.id},
            #{blog.userId},
            #{blog.shopId},
            #{blog.title},
            #{blog.images},
            #{blog.content},
            #{blog.liked}',
            #{blog.comments},
            #{blog.createTime},
            #{blog.updateTime},
    </insert>
</mapper>
